# Copy to .env and fill in locally — never commit .env
ANTHROPIC_API_KEY=
REDIS_URL=
SENTRY_DSN=
DEEPGRAM_API_KEY=
# Hosted Arize only — leave blank when using self-hosted Phoenix (Phase 5)
ARIZE_API_KEY=
ARIZE_SPACE_KEY=
