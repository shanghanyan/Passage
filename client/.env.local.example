# Public browser-side Sentry DSN — safe to expose in the client bundle.
# This is NOT the server SENTRY_DSN in /server/.env.
# Use the same project DSN as server SENTRY_DSN (browser SDK is safe to expose).
VITE_SENTRY_CLIENT_DSN=
